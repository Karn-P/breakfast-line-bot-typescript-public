{
  "name": "breakfast-line-bot-typescript",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "clean": "rm -rf dist",
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon ./src/server.ts",
    "start": "yarn run clean && yarn run build && node dist/server.js"
  },
  "license": "MIT",
  "dependencies": {
    "@line/bot-sdk": "^7.5.0",
    "dotenv": "^16.0.1",
    "express": "^4.18.1",
    "googleapis": "^105.0.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.13",
    "@types/node": "^18.0.6",
    "nodemon": "^2.0.19",
    "ts-node": "^10.9.1",
    "typescript": "^4.7.4"
  }
}
